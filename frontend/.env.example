# 이 파일을 .env.local 로 복사한 뒤 값을 채운다. (.env.local 은 git에 올라가지 않는다)
#
# ⚠️ VITE_ 로 시작하는 값은 빌드 시 브라우저 번들에 그대로 박힌다.
#    사용자가 개발자 도구로 전부 볼 수 있으므로 비밀 값은 절대 넣지 않는다.
#    REST API 키 · client secret · JWT 비밀키는 백엔드 .env 에만 둔다.

# ── 백엔드 연결 ─────────────────────────────────────────────
# 배포 백엔드를 쓸 때. 로컬 백엔드면 두 줄 다 지운다 (기본값 http://localhost:8080/v1)
# 배포 서버 CORS가 http://localhost:5173만 허용하는데 dev 서버는 https라서
# 직접 호출하면 403이 된다. 그래서 vite dev 프록시를 거친다.
VITE_API_BASE_URL=/v1
VITE_API_PROXY_TARGET=https://starttoo.duckdns.org

# ── 소셜 로그인 ─────────────────────────────────────────────
# 카카오 개발자 콘솔 > 앱 키 > "JavaScript 키"  (REST API 키 아님!)
# 이 키는 브라우저에 노출되는 것이 정상이며, 등록된 도메인에서만 동작한다.
# 콘솔에 함께 등록할 것:
#   [앱 키 > JavaScript SDK 도메인]   : https://localhost:5173
#                                      https://starttoo.duckdns.org
#   [카카오 로그인 > Redirect URI]    : https://localhost:5173/oauth/kakao/callback
#                                      https://starttoo.duckdns.org/oauth/kakao/callback
#   [카카오 로그인] 활성화 ON
# 도메인을 등록하지 않으면 JavaScript 키 자체가 동작하지 않는다.
VITE_KAKAO_JS_KEY=

# ── 도안 추출 AI 서버 (선택) ─────────────────────────────────
# /extract 페이지가 사진을 보내는 곳. 미설정 시 /ai-service/api/v1 —
# 배포에서는 nginx가, dev에서는 vite 프록시가 AI 서버로 넘긴다.
# VITE_EXTRACT_API_BASE_URL=
#
# 로컬에 AI 서버를 직접 띄웠다면 프록시가 향할 곳만 바꾼다 (경로는 그대로 /ai-service).
# 미설정 시 VITE_API_PROXY_TARGET(=배포 nginx)으로 간다.
# VITE_AI_PROXY_TARGET=http://127.0.0.1:8791

# ── AR QR 진입 주소 (로컬 테스트용, 선택) ──────────────────
# 폰이 QR로 들어갈 주소의 오리진. 기본은 PC가 열려 있는 주소 그대로다.
# 로컬에서 폰 테스트를 하려면 PC는 localhost로 열어야 소셜 로그인이 되는데
# (리다이렉트 URI가 카카오/구글 콘솔에 등록된 값이어야 한다) 폰은 localhost로
# 들어올 수 없다. 그래서 QR만 PC의 LAN 주소를 가리키게 한다.
# 값은 vite 시작 로그의 Network 줄에 찍힌 실제 Wi-Fi 주소로 (Wi-Fi 바뀌면 같이 바뀜).
# 폰이 여는 AR 페이지는 로그인이 필요 없어서 이것만으로 충분하다.
# VITE_AR_JOIN_ORIGIN=https://172.20.10.5:5173

# ── UI QA용 목업 데이터 (선택) ─────────────────────────────
# API가 빈 배열을 반환할 때 커뮤니티·피드·타투이스트·마이페이지에
# 샘플 데이터를 표시하려는 경우에만 true로 설정한다. 기본값은 비활성화다.
VITE_ENABLE_QA_MOCK_DATA=false
