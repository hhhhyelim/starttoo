venv
.venv
__pycache__
*.pyc
.git
.idea

# 테스트는 이미지에 넣지 않는다 (합성 데이터 생성기까지 들어가면 불필요하게 커진다).
# Dockerfile 이 coverup/ 만 COPY 하므로 사실 중복이지만 의도를 명시해 둔다.
tests

# 로컬에서 만든 특징 파일. 이미지에 굽지 않는다 — 볼륨으로 마운트한다.
coverup_store

# The SD1.5 weights are supplied through an ai-api bind mount.
api_server/vendor/tattoo_generator_sd15/models/stable-diffusion-v1-5/
